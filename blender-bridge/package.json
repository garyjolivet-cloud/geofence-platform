{
  "name": "object-studio-blender-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local-only Blender control bridge for Object Studio — an MCP server plus an HTTP server the web module's Generate button calls. Never deployed; runs on the same machine as Blender.",
  "main": "mcp-server.js",
  "scripts": {
    "mcp": "node mcp-server.js",
    "bridge": "node http-bridge.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  }
}
